The steps for the execution of the program are :

gcc <filename.c> -o <output_file_name>
./output_file_name.out

The program just uses normal C libraries and 
two extra libraries of <emmintrin.h> and <time.h> 
which are also present in the build-essential 
package in linux.